name: Unit Tests

on:
  push:
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest
    strategy:
      matrix:
        dotnet-version: [ '9.x' ]

    steps:
    - uses: actions/checkout@v4
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: ${{ matrix.dotnet-version }}
    - name: Restore dependencies
      run: dotnet restore
    - name: Build
      run: dotnet build --no-restore
    - name: Test
      run: dotnet test --filter Category=UnitTest --no-build --verbosity normal --logger trx --results-directory "UnitTestResults-${{ matrix.dotnet-version }}"
    - name: Upload results
      uses: actions/upload-artifact@v4
      with:
        name: dotnet-unit-test-results-${{ matrix.dotnet-version }}
        path: UnitTestResults-${{ matrix.dotnet-version }}
      if: ${{ always() }}
